# LNOS AppArmor Configuration
# Section 40 of LNOS Specification

# Enable AppArmor in kernel parameters
# Add: ls=apparmor to kernel cmdline

# Set AppArmor to enforce mode by default
# Profiles in /etc/apparmor.d/ are enforced
# Use aa-complain for debugging specific profiles
